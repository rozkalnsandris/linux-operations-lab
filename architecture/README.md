# Architecture & Request Flows

Store simple diagrams and explanations that support troubleshooting.

Planned Week 3 artifact:

**client → DNS → routing/gateway → firewall → reverse proxy/service → application → database/dependency**

Diagrams should explain where evidence can be collected at each layer and how failures differ.
